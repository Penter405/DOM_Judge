from datetime import datetime




















"""result=[]
def get_day(x):
    #0=sumday
    #1=monday
    return x%7
def to_time(x,y):
    result=dict()
    bot=y.split(":")
    result["min"]=int(bot[1])
    result["hr"]=int(bot[0])
    bot=x.split("/")
    result["y"]=int(bot[0])
    result["m"]=int(bot[1])
    result["d"]=int(bot[2])
for _ in range(int(input())):
    s=input().split()
    if s[0]=="Library":
        now=to_time(s[1],s[2])
        
    else:
        now=to_time(s[1],s[2])
"""
