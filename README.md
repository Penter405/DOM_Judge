### where to put exe
move the exe you want out of bot scope. the exe will make a folder in same scope with itself
### what exe i might want
auto_clone uses git and c++ file.
auto_fetch uses windows built in tool.
### initial setting
i have initialized struct to built in tool in root, need git and c++ in bot folder
### first push in your downloaded folder
```
git push -u origin main
```